import { useState } from "react";

export default function Navbar() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="sticky top-4 mx-auto max-w-4xl w-[90%] z-50 rounded-2xl sm:rounded-full bg-white/80 backdrop-blur-md shadow-lg border border-white/20 text-gray-800 transition-all duration-300">
      {/* Top Bar */}
      <div className="flex justify-between items-center px-6 sm:px-8 h-16">
        <div className="font-bold text-xl tracking-tight text-indigo-600">
          Task Master
        </div>
        
        {/* Desktop Buttons (Hidden on small screens) */}
        <div className="hidden sm:flex gap-2 sm:gap-4">
          <button className="px-4 py-2 rounded-full hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            My Tasks
          </button>
          <button className="px-4 py-2 rounded-full hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            QUADRIENT
          </button>
          <button className="px-4 py-2 rounded-full hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            SETTINGS
          </button>
        </div>

        {/* Mobile Hamburger Button (Visible only on small screens) */}
        <div className="sm:hidden flex items-center">
          <button 
            onClick={() => setIsOpen(!isOpen)}
            className="p-2 text-indigo-600 hover:bg-indigo-50 rounded-full cursor-pointer transition-colors"
          >
            {/* We draw a hamburger icon or a close (X) icon depending on whether the menu is open */}
            <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              {isOpen ? (
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
              ) : (
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
              )}
            </svg>
          </button>
        </div>
      </div>

      {/* Mobile Menu Dropdown (Shows when 'isOpen' is true) */}
      {isOpen && (
        <div className="sm:hidden px-4 pb-4 flex flex-col gap-2 border-t border-indigo-100 pt-2 animate-in fade-in slide-in-from-top-4">
          <button className="w-full text-left px-4 py-2 rounded-xl hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            My Tasks
          </button>
          <button className="w-full text-left px-4 py-2 rounded-xl hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            QUADRIENT
          </button>
          <button className="w-full text-left px-4 py-2 rounded-xl hover:bg-indigo-50 text-sm font-medium transition-colors cursor-pointer">
            SETTINGS
          </button>
        </div>
      )}
    </nav>
  );
}
